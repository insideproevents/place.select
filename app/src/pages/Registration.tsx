import RegistrationForm from '@/components/RegistrationForm';

export default function RegistrationPage() {
  return (
    <section className="min-h-[100dvh] flex items-center justify-center bg-[#0A0A0A] py-12">
      <RegistrationForm />
    </section>
  );
}